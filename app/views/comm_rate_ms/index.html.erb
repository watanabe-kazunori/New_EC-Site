<p id="notice"><%= notice %></p>

<h1>Listing Comm Rate Ms</h1>

<table>
  <thead>
    <tr>
      <th>Comm rate ver</th>
      <th>Comm rate</th>
      <th>Start date</th>
      <th>End date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comm_rate_ms.each do |comm_rate_m| %>
      <tr>
        <td><%= comm_rate_m.comm_rate_ver %></td>
        <td><%= comm_rate_m.comm_rate %></td>
        <td><%= comm_rate_m.start_date %></td>
        <td><%= comm_rate_m.end_date %></td>
        <td><%= link_to 'Show', comm_rate_m %></td>
        <td><%= link_to 'Edit', edit_comm_rate_m_path(comm_rate_m) %></td>
        <td><%= link_to 'Destroy', comm_rate_m, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comm rate m', new_comm_rate_m_path %>
